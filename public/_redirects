# Redirect old color URL structure for backward compatibility
/color/:hex([0-9a-fA-F]{3,6})   /html-color-picker?hex=:hex  302

# Only redirect specific known problematic patterns
# Add specific hex patterns here if needed, but avoid broad patterns
# that might catch static asset requests